{
  "name": "mas-smartiq-advice-poc",
  "version": "1.0.0",
  "description": "Azure Function for NZ financial projections with deterministic and Monte Carlo methods",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "npm run build && node dist/src/test/vegaValidation.test.js && node test-projection-method.js"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
